<style amp-custom>
    :root {
        /* Kingfisher Daisy */
        --color-primary-darkest: #251330;
        --color-primary-darker: #562d6e;
        --color-primary: #643480;
        --color-primary-lighter: #8043a4;
        --color-primary-lightest: #8e4bb6;

        /* Veridian */
        --color-secondary: #348064;
        --color-secondary-lighter: #41a07d; 

        /* Hot Curry */
        --color-tertiary: #806434;
        --color-tertiary-lighter: #908573;
        --color-tertiary-lightest: #b68e4b;

        /* Eerie Black */
        --color-dark: #1b1b1b;

        --font-color-light: #dedede;
        --font-color-lighter: #f2f2f2;

        --font-size-primary: 18px;
        --line-height-primary: 24px;

        --font-primary: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif;
    }

    html,
    body {
        margin: 0;
        padding: 0;
        font-family: var(--font-primary);
        overflow-x: hidden;
    }

    p,
    a,
    pre,
    span {
        font-size: var(--font-size-primary);
        line-height: var(--line-height-primary);
    }

    a {
        text-decoration: none;
        color: var(--font-color-light);
    }

    a:hover {
        color: var(--font-color-lighter);
    }

    footer {
        width: 100%;
        height: 4em;
        background-color: var(--color-dark);
        text-align: center;
        padding-top: 1em;
        border-top: 3px solid var(--color-primary);
    }

    footer small {
        font-size: 13px;
        color: var(--font-color-light);
        margin: 1em;
    }

    footer small a {
        font-size: 13px;
        color: var(--font-color-light);
    }

    footer small a:hover {
        background-color: var(--color-primary);
    }

    ::-webkit-scrollbar {
        width: 10px;
    }

    ::-webkit-scrollbar-track {
        background-color: var(--font-color-light);
    }

    ::-webkit-scrollbar-thumb {
        background-color: var(--color-primary);
    }

    .container {
        max-width: 590px;
        min-height: 70vh;
        margin: auto;
    }

    .btn {
        text-decoration: none;
        color: white;
        background-color: var(--color-secondary);
        padding: .5em 1em;
        border-radius: .25em;
    }

    .btn:hover {
        background-color: #41a07d;
    }

    /* Nav Components */
    #top-nav {
        display: block;
        width: 100%;
        height: 48px;
        background-color: var(--color-dark);
        border-bottom: 3px solid var(--color-secondary-lighter);
    }

    .nav-link {
        float: left;
        padding: 16px 12px 8px 12px;
        font-size: var(--font-size-primary);
    }

    .nav-link:hover {
        color: var(--font-color-lighter);
        background-color: var(--color-primary);
    }

    .dropdown {
        float: left;
        overflow: hidden;
    }

    .dropdown .dropbtn {
        font-size: var(--font-size-primary);
        border: none;
        outline: none;
        color: white;
        padding: 14px 16px;
        background-color: inherit;
        font-family: inherit;
        margin: 0;
    }

    .navbar a:hover, .dropdown:hover .dropbtn {
        background-color: var(--color-secondary-lighter);
    }

    .dropdown-content {
        display: none;
        position: absolute;
        background-color: #f9f9f9;
        min-width: 160px;
        box-shadow: 0px 8px 16px 0px rgba(0,0,0,0.2);
        z-index: 1;
    }

    .dropdown-content a {
        float: none;
        color: black;
        padding: 12px 16px;
        text-decoration: none;
        display: block;
        text-align: left;
    }

    .dropdown-content a:hover {
        background-color: #ddd;
    }

    .dropdown:hover .dropdown-content {
        display: block;
    }

    .thumbnail {
        width: 90px;
        height: 88px;
        border-radius: 45px;
        filter: grayscale(100%);
    }

    #cover-page {
        background-color: var(--color-primary);
        background-image: linear-gradient(to top right, var(--color-primary), var(--color-secondary));
        background-repeat: no-repeat;
        box-shadow: inset 0 0 5rem rgba(0, 0, 0, .5);
        overflow: hidden;
        text-align: center;
    }

    #cover {
        max-width: 30em;
        padding: 20vh 1em 50vh;
        margin: auto;
        color: var(--font-color-lighter);
    }

    #cover-footer {
        position: fixed;
        bottom: 0;
        background-color: var(--color-dark);
        height: 2em;
        padding-top: 0;
        padding-bottom: 1em;
        border-top: 3px solid var(--color-primary);
    }

    .lead a {
        color: var(--color-tertiary-lightest);
    }

    .lead a:hover {
        color: var(--font-color-light);
    }

    .container {
        margin: 1em auto;
    }

    #tracker-table {
        display: block;
        border-collapse: collapse;
        width: 100%;
    }

    #tracker-table td,
    th {
        border: 1px solid #ddd;
        padding: 8px;
    }

    #tracker-table tr:nth-child(even) {
        background-color: var(--font-color-light);
    }

    #tracker-table th {
        padding-top: 12px;
        padding-bottom: 12px;
        text-align: left;
        background-color: var(--color-secondary);
        color: white;
    }

    #opinion {
        margin-left: 2em;
    }

    #opinion pre, span {
        font-family: Cambria, Garamond, Times;
        display: inline;
    }

    #opinion span {
        color: blue;
    }

    @media only screen and (max-width: 600px) {
        .container {
            max-width: 85%;
            margin: auto;
        }

        .container #opinion {
            overflow-x: scroll;
        }
    }
</style>