<style amp-custom>
    :root {
        /* Kingfisher Daisy */
        --color-primary: #643480;

        /* Veridian */
        --color-secondary: #348064;

        /* Hot Curry */
        --color-tertiary: #806434;
        --color-tertiary-lighter: #908573;

        /* Eerie Black */
        --color-dark: #1b1b1b;

        --font-color-light: #dedede;
        --font-color-lighter: #f2f2f2;

        --font-size-primary: 18px;
        --line-height-primary: 24px;

        --font-primary: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif;
        --font-decorative: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif;
    }

    html,
    body {
        margin: 0;
        padding: 0;
        font-family: var(--font-primary);
        overflow-x: hidden;
    }

    p,
    a,
    pre,
    span {
        font-size: var(--font-size-primary);
        line-height: var(--line-height-primary);
    }

    h1,
    h2,
    h3 {
        font-weight: 300;
        font-family: var(--font-decorative);
    }

    a {
        text-decoration: none;
        color: var(--font-color-light);
    }

    a:hover {
        color: var(--font-color-lighter);
    }

    footer {
        width: 100%;
        height: 4em;
        background-color: var(--color-dark);
        text-align: center;
        padding-top: 1em;
    }

    footer small {
        font-size: 13px;
        color: var(--font-color-light);
        margin: 1em;
    }

    footer small a {
        font-size: 13px;
        color: var(--font-color-light);
    }

    .container {
        max-width: 590px;
        min-height: 70vh;
        margin: auto;
    }

    .btn {
        text-decoration: none;
        color: white;
        background-color: var(--color-secondary);
        padding: .5em 1em;
        border-radius: .25em;
    }

    .btn:hover {
        background-color: #41a07d;
    }


    /* Nav Components */
    #top-nav {
        display: block;
        width: 100%;
        height: 48px;
        background-color: var(--color-dark);
    }

    .nav-link {
        float: left;
        padding: 16px 12px 8px 12px;
        font-size: var(--font-size-primary);
    }

    .nav-link:hover {
        color: var(--font-color-lighter);
    }

    .thumbnail {
        width: 90px;
        height: 88px;
        border-radius: 45px;
        filter: grayscale(100%);
    }

    #cover-page {
        background-color: var(--color-primary);
        background-image: linear-gradient(to top right, var(--color-primary), var(--color-secondary));
        background-repeat: no-repeat;
        box-shadow: inset 0 0 5rem rgba(0, 0, 0, .5);
        overflow: hidden;
        text-align: center;
    }

    #cover {
        max-width: 30em;
        padding: 20vh 1em 50vh;
        margin: auto;
        color: var(--font-color-lighter);
    }

    #cover-footer {
        position: fixed;
        bottom: 0;
        background-color: var(--color-dark);
        height: 2em;
        padding-top: 0;
        padding-bottom: .5em;
    }

    .lead a {
        color: var(--color-tertiary);
    }

    .lead a:hover {
        color: var(--color-tertiary-lighter);
    }

    @media only screen and (max-width: 600px) {
        .container {
            max-width: 85%;
            margin: auto;
        }
    }
</style>